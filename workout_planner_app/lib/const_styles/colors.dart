import 'package:flutter/material.dart';

const HSubtitleColor = Color(0xFF7E7E7E); // Neutral soft subtitle
const HMainColor = Color(0xFF1B1B1B); // Dark for text
const HMainWhiteColor = Color(0xFFFFFFFF); // Pure white

// Energetic gradient: top is lighter, bottom is deeper
const HGradientTopColor = Color(0xFFFF8A65); // Light coral (warm & friendly)
const HGradientBottomColor = Color(0xFFFF7043); // Deeper coral

const HCardBackgroundColor = Color(0xFFFDFDFD); // Almost white for soft UI
final HCardButtonColor = Colors.orange[100]; // Subtle orange tint

const HMainPinkColor = Color(0xFFFF4081); // Vibrant pink accent (Material pink A200)
const HAccentGreenColor = Color(0xFF66BB6A); // Fresh success color
const HWarningRedColor = Color(0xFFEF5350); // Soft red for warnings
